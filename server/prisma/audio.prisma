datasource db {
    provider = "sqlite"
    url = env("DATABASE_URL")
}

model Audio {
    id Int @id @default(autoincrement())
    alt String
    path String
    length Float
    created_at DateTime @default(now())
}